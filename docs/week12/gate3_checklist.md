# Gate 3 체크리스트 — Phase 1-A 완료 (12주차)

**기준일:** 2026-06-09  
**평가 기준:** 다음 4개 조건이 모두 충족되어야 Gate 3 통과

---

## 체크리스트

### ✅ 1. 친구 1명이 도움 없이 가입하고 진단 8문제까지 완료

| 항목 | 상태 |
|------|------|
| 회원가입 (이메일 인증 포함) | ✅ |
| 온보딩 (목표 점수 선택) | ✅ |
| 진단 자동 시작 (`/chat?new=true`) | ✅ |
| 진단 N/8 뱃지 표시 (1~8번 전체) | ✅ |
| 8문제 완료 → 진단 리포트 출력 | ✅ |

**1차 사용성 테스트 결과 (2026-06-09, 친구 1명):**
- 진단 8문제 완료 확인
- 발견 버그 2건:
  - 4번 보기 회색 박스 오감지 → **수정 완료** (commit `cec5613`, `_SQL_IN_OPTION` 정규식 `^` 앵커 방식으로 교체)
  - 모바일 레이아웃 잘림 → 수정 예정

**2차 사용성 테스트 결과 (2026-06-10, 친구 2명째):**
- 온보딩 → 진단 8문제 → 결과 화면 전체 흐름 막힘 없이 완주
- 태블릿 정상 동작 확인
- 발견 버그 1건: pending 문제 있을 때 카테고리 변경 요청 시 무반응 → **수정 완료** (commit `cbc6d20`)
- 기능 제안: 진단 문제를 분야별 2~3문제로 늘리면 좋겠다는 의견 (설계 논의 사항)

---

### ✅ 2. 사용자별 학습 데이터가 영속 저장되고 다시 접속 시 이어 학습

| 항목 | 상태 |
|------|------|
| Supabase PostgreSQL 연결 | ✅ |
| LangGraph PostgresSaver checkpointing | ✅ |
| `thread_id = user.id` 연결 | ✅ |
| 재접속 시 이전 대화 이어짐 | ✅ |

**구현 파일:** `backend/app/db/checkpointer.py`  
**주의:** Render 환경변수 `DATABASE_URL`이 설정되지 않으면 MemorySaver fallback — Render 대시보드에서 반드시 확인

---

### ✅ 3. HTTPS 도메인으로 외부에서 접속 가능

| 항목 | 상태 |
|------|------|
| 프론트엔드 HTTPS 배포 (Vercel) | ✅ `https://sqld-tutor.vercel.app` |
| 백엔드 HTTPS 배포 (Render) | ✅ `https://sqld-tutor.onrender.com` |
| 커스텀 도메인 (sqld-tutor.com) | — 브랜딩 목적, Gate 3 필수 조건 아님 |

**판단 근거:** Gate 3 기준은 "HTTPS 도메인으로 외부 접속 가능"이며 `sqld-tutor.vercel.app`은 HTTPS + 외부 접속 모두 충족. 커스텀 도메인은 기술 조건과 무관한 브랜딩 요소로 판단.

---

### ✅ 4. 모든 사용자 행동이 데이터베이스에 로깅됨

| 이벤트 | 로깅 위치 | 상태 |
|--------|-----------|------|
| `session_start` | 새 세션 시작 시 | ✅ |
| `question_answered` | 문제 채점 완료 시 (정답 여부, 카테고리, 난이도) | ✅ |
| `feature_used` | explain / diagnose / sql / review 진입 시 | ✅ |

**구현 파일:** `backend/app/analytics.py`, `backend/app/db/` → Supabase `user_events` 테이블

---

## 전체 판정

| 항목 | 판정 |
|------|------|
| 1. 친구 1명 테스트 | ✅ (1차 + 2차 완료) |
| 2. 영속 저장 + 이어 학습 | ✅ |
| 3. HTTPS 도메인 접속 | ✅ |
| 4. 행동 로깅 | ✅ |

**Gate 3 상태: 통과** ✅ (2026-06-10 확정)
